rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TÜM koleksiyonlar için authentication gerekli (daha basit ve çalışır)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
